export default function Navbar({children, }) {
  return (
    <nav className="w-full h-10 bg-slate-50 text-sky-500 flex col-auto shadow"
    id="aside">
      ini adalah navbar {children}
    </nav>
    )
}