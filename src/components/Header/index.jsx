export default function Header({children, }) {
  return (
    <header className="w-full h-10 bg-slate-50 text-sky-500 flex col-auto shadow"
    id="aside">
      ini adalah header {children}
    </header>
    )
}